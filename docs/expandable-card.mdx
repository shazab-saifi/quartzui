# Expandable Card

import { CodeBlock } from './CodeBlock';
import LiveCode from './LiveCode';

An expandable card component with a smooth animation.

## Live Preview

<div className="not-prose w-full overflow-x-auto">
  <LiveCode code={`<ExpandableCard />`} />
</div>

## Usage

To use the expandable card component, import it from `@/components/ExpandableCard`.

<CodeBlock code={`import ExpandableCard from '@/components/ExpandableCard';`} />

## Props

The expandable card component does not take any props.
