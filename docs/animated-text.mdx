import { CodeBlock } from './CodeBlock';
import LiveCode from './LiveCode';
import InstallToggle from './InstallToggle';
import { PropsTable } from '@/ui/docs/props-table';

## Animated Text

A modern React component that animates the appearance of each word for dynamic, attractive text transitions. Perfect for headlines, hero sections, or anywhere dynamic, eye-catching text is needed.  
Inspired by popular animated heading techniques found in top-tier UIs.

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Live Preview

<div className="not-prose w-full overflow-x-auto">
  <LiveCode code={`<AnimatedText className="text-2xl">This is an animated text.</AnimatedText>`} />
</div>

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Installation

You can install the AnimatedText component with the following CLI command, or copy the implementation manually:

<InstallToggle
  cliCode="npx shadcn@latest add http://localhost:3000/r/animated-text.json"
  manualCode={`'use client';

  import { cn } from '@/lib/utils';
  import { motion } from 'motion/react';
  
  type AnimatedTextProps = {
    children: string;
    className?: string;
  };
  
  const AnimatedText = ({
    children,
    className,
  }: AnimatedTextProps) => {
    return (
      <div className="flex max-w-4xl flex-wrap justify-center">
        {children.split(' ').map((word, idx) => (
          <motion.span
            initial={{ opacity: 0, y: 10 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 1, delay: idx * 0.1, ease: 'easeInOut' }}
            key={idx}
            className={cn('inline-block text-4xl font-bold', className)}
            style={{ display: 'inline-block', willChange: 'transform' }}
          >
            {word}&nbsp;
          </motion.span>
        ))}
      </div>
    );
  };
  
  export default AnimatedText;
`}
/>

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Import

To use the AnimatedText component, import it in your code:

<CodeBlock code={`import AnimatedText from '@/components/AnimatedText';`} />

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Props

The AnimatedText component accepts the following props:

<PropsTable
  rows={[
    {
      prop: 'children',
      type: 'string',
      default: '-',
      description: 'The text to animate, one word at a time.',
      required: 'Yes',
    },
    {
      prop: 'className',
      type: 'string',
      default: '-',
      description: 'Utility classes for controlling text style/size/weight.',
      required: 'No',
    },
  ]}
/>

<CodeBlock
  code={`type AnimatedTextProps = {
    children: string;
    className?: string;
};`}
/>

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Example usage

### Basic usage

<CodeBlock code={`<AnimatedText>This is an animated text.</AnimatedText>`} />

### With custom font size and color

<CodeBlock code={`<AnimatedText className="text-3xl text-blue-700">Big & Blue</AnimatedText>`} />

### Uppercase with spacing

<CodeBlock code={`<AnimatedText className="uppercase tracking-widest">Fancy Style Reveal</AnimatedText>`} />

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Features

- **Staggered animation:** Words appear in sequence for a modern animation effect
- **Customizable:** Style with any className, including font size, color, and more
- **Type-safe:** TypeScript definitions for strict props
- **Easy integration:** Plug-and-play for Next.js, React, or any modern React stack

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Customization

- **Animation speed:** Adjust the delay interval or duration in the `motion.span` logic
- **Text style:** Pass className for custom size, color, weight, etc.
- **Layout:** By default text is wrapped and centered; change container flex classes for layout variants
- **Advanced:** Swap the animation for your own `framer-motion` or motion library variants

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Troubleshooting

- Ensure the `children` prop is a **string** (not JSX/element), or the effect will not render as intended.
- If animation does not appear, check that the motion library you use is properly installed and imported.
- For custom fonts or styles, ensure your Tailwind or global CSS config is loaded.

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

