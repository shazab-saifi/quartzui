import { CodeBlock } from './CodeBlock';
import LiveCode from './LiveCode';
import InstallToggle from './InstallToggle';
import { PropsTable } from '@/ui/docs/props-table';

# Tilt Card

A modern, interactive React component that provides a 3D "tilt" effect as the user moves their mouse across the card, animating rotation for a tactile, playful experience. Perfect for showcasing product cards, interactive UI elements, hero sections, and dashboards.
Inspired by clean motion-driven web UI patterns.

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Live Preview

<div className="not-prose w-full overflow-x-auto">
  <LiveCode code={`<TiltCard>
  <div className="flex flex-col items-center justify-center p-4 bg-linear-to-br from-neutral-100 to-neutral-200 dark:from-neutral-900 border border-neutral-100 dark:border-neutral-800 dark:to-neutral-950 h-full space-y-4 text-center">
    <span className="block dark:hidden">
      <img
        src="https://res.cloudinary.com/dlpjh3fcx/image/upload/v1759296296/quartzui-light_ytrk9a.svg"
        alt="quartz-ui"
        className="size-16"
      />
    </span>
    <span className="hidden dark:inline">
      <img
        src="https://res.cloudinary.com/dlpjh3fcx/image/upload/v1759296296/quartzui-light_ytrk9a.svg"
        alt="quartz-ui"
        className="size-16"
      />
    </span>
    <h3 className="text-xl font-bold text-neutral-900 dark:text-neutral-100">
      Beautiful 3D Card
    </h3>
    <p className="text-sm text-neutral-600 dark:text-neutral-300">
      This card tilts with your mouse and adapts to light and dark themes.
      Add your own content here!
    </p>
    <button className="mt-2 rounded-lg bg-neutral-950 px-4 py-2 text-sm font-semibold text-white shadow hover:bg-neutral-800 dark:bg-neutral-100 dark:text-neutral-950 dark:hover:bg-neutral-200">
      Get Started
    </button>
  </div>
</TiltCard>`} />
</div>

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Installation

You can install the TiltCard component with the following CLI command, or copy the implementation manually:

<InstallToggle
  cliCode="npx shadcn@latest add https://quartzui.shazab.site/registry/tilt-card.json"
  manualCode={`'use client';

import { motion, useMotionValue, useSpring, useTransform } from 'motion/react';
import { useRef } from 'react';

export const TiltCard = ({ children }: { children: React.ReactNode }) => {
  const ref = useRef<HTMLDivElement | null>(null);
  const x = useMotionValue(0);
  const y = useMotionValue(0);
  const mouseXSpring = useSpring(x);
  const mouseYSpring = useSpring(y);

  const rotateX = useTransform(
    mouseYSpring,
    [-0.5, 0.5],
    ['8.5deg', '-8.5deg']
  );

  const rotateY = useTransform(
    mouseXSpring,
    [-0.5, 0.5],
    ['-8.5deg', '8.5deg']
  );

  const handleMouseMove = (e: React.MouseEvent<HTMLDivElement, MouseEvent>) => {
    if (!ref.current) return;
    const rect = ref.current.getBoundingClientRect();

    const mouseX = e.clientX - rect.left;
    const mouseY = e.clientY - rect.top;

    const xPct = mouseX / rect.width - 0.5;
    const yPct = mouseY / rect.height - 0.5;

    x.set(xPct);
    y.set(yPct);
  };

  const handleMouseLeave = () => {
    x.set(0);
    y.set(0);
  };

  return (
    <motion.div
      ref={ref}
      onMouseMove={handleMouseMove}
      onMouseLeave={handleMouseLeave}
      style={{ transformStyle: 'preserve-3d', rotateX, rotateY }}
      className="h-96 w-72 rounded-xl bg-linear-to-br from-neutral-200/50 to-neutral-400/50 dark:from-neutral-900/60 dark:to-neutral-800/60"
    >
      <div
        style={{
          transform: 'translateZ(75px)',
          transformStyle: 'preserve-3d',
        }}
        className="absolute inset-3 overflow-hidden rounded-xl shadow-2xl"
      >
        {children}
      </div>
    </motion.div>
  );
};
`}
/>

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Import

To use the TiltCard component, import it in your code:

<CodeBlock code={`import { TiltCard } from '@/components/TiltCard';`} />

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Props

The TiltCard component accepts the following props:

<PropsTable
  rows={[
    {
      prop: 'children',
      type: 'React.ReactNode',
      default: '-',
      description: 'The content to display inside the tilt card.',
      required: 'Yes',
    },
  ]}
/>

<CodeBlock
  code={`
export const TiltCard = ({ children }: { children: React.ReactNode }) => { ... }
`}
/>

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Example usage

### Basic usage

<CodeBlock code={`<TiltCard>
    <div className="text-center">
      <h2 className="text-xl font-bold">Tilt Me</h2>
      <p>Move your mouse over me.</p>
    </div>
</TiltCard>`} />

### With custom content

<CodeBlock code={`<TiltCard>
    <div className="flex flex-col items-center justify-center p-6 space-y-2">
      <img src="https://avatars.githubusercontent.com/u/1?v=4" alt="github" className="w-14 h-14 rounded-full shadow" />
      <h3 className="text-lg font-semibold">Profile Tilt</h3>
      <p className="text-neutral-600 dark:text-neutral-300">Even avatars and stacked layouts can tilt in 3D!</p>
    </div>
</TiltCard>`} />

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Features

- **3D tilt animation:** Card moves in response to your mouse for a high-tactile feel.
- **Physics-based:** Uses [motion.dev](https://motion.dev/) and spring-based animation.
- **Composable:** Accepts any child content—brand, UI, or marketing.
- **Theme-aware:** Card appearance adapts to light and dark modes.

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Customization

- Adjust inner/outer card markup and styles for your brand or design system.
- Change the tilt effect's angle/intensity by tuning the transform/spring.
- Use with any React children for product cards, info blocks, or playful UI.

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />

## Troubleshooting

- You must have [motion.dev](https://motion.dev/) installed for animation support.
- If the card doesn't tilt, check that your parent/container doesn't block pointer events.
- Adjust or remove the `h-96 w-72` classes to match your layout.
- If you see odd clipping or shadow bugs, make sure overflow and Z stacking are correct and your children aren't absolutely positioned outside the card div.

<hr className="border-0 border-t border-dotted border-neutral-200 dark:border-neutral-800 my-6" />
