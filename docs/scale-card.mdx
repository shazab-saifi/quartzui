# Scale Card

import { CodeBlock } from './CodeBlock';
import LiveCode from './LiveCode';

A card component that scales on hover.

## Live Preview

<div className="not-prose">
  <LiveCode code={`<ScaleCard />`} />
</div>

## Usage

To use the scale card component, import it from `@/components/ScaleCard`.

<CodeBlock code={`import ScaleCard from '@/components/ScaleCard';`} />

## Props

The scale card component does not take any props.
