# Banner

import { CodeBlock } from './CodeBlock';
import LiveCode from './LiveCode';

A responsive banner component to enhance your application's visual appeal.

## Live Preview

<div className="not-prose w-full overflow-x-auto">
  <LiveCode code={`<Banner />`} />
</div>

## Usage

To use the banner component, import it from `@/components/Banner`.

<CodeBlock code={`import Banner from '@/components/Banner';`} />

## Props

The banner component does not take any props.
