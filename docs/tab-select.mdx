# Tab Select

import { CodeBlock } from './CodeBlock';
import LiveCode from './LiveCode';

A tab selection component with a smooth animation.

## Live Preview

<div className="not-prose w-full overflow-x-auto">
  <LiveCode code={`<TabSelect tabs={['Home', 'Comoponents', 'Pricing']} />`} />
</div>

## Usage

To use the tab select component, import it from `@/components/TabSelect`.

<CodeBlock code={`import TabSelect from '@/components/TabSelect';`} />

## Props

The tab select component takes a `tabs` prop, which is an array of strings.

<CodeBlock
  code={`
<TabSelect tabs={['Home', 'Comoponents', 'Pricing']} />
`}
/>
