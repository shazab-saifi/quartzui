# Price Switcher

import { CodeBlock } from './CodeBlock';
import LiveCode from './LiveCode';

A component that animates a price change between monthly and annually.

## Live Preview

<div className="not-prose">
  <LiveCode code={`<PriceSwitcher />`} />
</div>

## Usage

To use the price switcher component, import it from `@/components/PriceSwitcher`.

<CodeBlock code={`import PriceSwitcher from '@/components/PriceSwitcher';`} />

## Props

The price switcher component does not take any props.
