# Scroll Progress

import { CodeBlock } from './CodeBlock';
import LiveCode from './LiveCode';

A component that displays a progress bar indicating the scroll position of the page.

## Live Preview

<div className="not-prose">
  <LiveCode code={`<ScrollProgress />`} />
</div>

## Usage

To use the scroll progress component, import it from `@/components/ScrollProgress`.

<CodeBlock code={`import ScrollProgress from '@/components/ScrollProgress';`} />

## Props

The scroll progress component does not take any props.
